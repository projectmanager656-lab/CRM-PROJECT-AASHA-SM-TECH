import AdminLayout from '../components/AdminLayout';
import CalendarScheduler from '../../shared/CalendarScheduler';

export default function Calendar() { return <AdminLayout pageTitle="Calendar"><CalendarScheduler isAdmin /></AdminLayout>; }
