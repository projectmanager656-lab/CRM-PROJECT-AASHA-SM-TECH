// Error Layout Component
export default function ErrorLayout({ children }) {
  return (
    <div className="error-layout">
      {/* Error pages will be added in later phases */}
      {children}
    </div>
  );
}
