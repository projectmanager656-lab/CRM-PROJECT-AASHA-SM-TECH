import fs from 'fs';import path from 'path';import {Router} from 'express';import multer from 'multer';import {DocumentController} from '../controllers/DocumentController.js';import {authenticateToken,requirePermission} from '../middleware/auth.middleware.js';
const root=path.resolve('uploads/documents');fs.mkdirSync(root,{recursive:true});const upload=multer({storage:multer.diskStorage({destination:root,filename:(req,file,cb)=>cb(null,`${Date.now()}-${Math.random().toString(36).slice(2)}${path.extname(file.originalname).toLowerCase()}`)}),limits:{fileSize:10*1024*1024}});
const router=Router();router.use(authenticateToken);router.route('/').get(requirePermission('documents','documents','view'),DocumentController.list).post(requirePermission('documents','documents','create'),upload.single('file'),DocumentController.upload);router.get('/:id',requirePermission('documents','documents','view'),DocumentController.get);router.get('/:id/content',requirePermission('documents','documents','read'),DocumentController.download);router.delete('/:id',requirePermission('documents','documents','delete'),DocumentController.remove);export default router;
